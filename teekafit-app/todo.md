# Project TODO - Teekafit

- [x] تخطيط البنية الأساسية وإنشاء قائمة المهام (TODO)
- [x] بناء نموذج قاعدة البيانات (Database Schema) لتخزين التمارين، الوجبات، الأهداف، وسجل الـ Streak — `drizzle/schema.ts` + `drizzle/relations.ts` (لم تُشغّل الهجرة بعد، تحتاج `DATABASE_URL`)
- [x] تطوير واجهة الترحيب المتحركة (Animated Splash Screen) مع شعار "Tt" وتأثير النبض باللون الأخضر النيوني — `client/src/components/TeekafitSplashScreen.tsx`
- [ ] بناء نظام المصادقة وإدارة الملف الشخصي والسعرات المستهدفة (السكافولد جاهز عبر Manus OAuth، لكن غير مفعّل/غير مربوط بالشاشات الجديدة بعد)
- [x] تطوير لوحة التحكم الرئيسية (Dashboard) مع التحية الذكية، تقدم السعرات، وعداد الأيام المتتالية (Streak) — `client/src/pages/Dashboard.tsx` (بيانات تجريبية محلية)
- [x] بناء منشئ جدول التمارين (Workout Schedule Builder) مع تفاصيل التمارين والمجموعات — `client/src/pages/Workouts.tsx` (بيانات تجريبية محلية)
- [x] تطوير متتبع التغذية (Nutrition Tracker) لتسجيل الوجبات وتتبع البروتينات، الكربوهيدرات، والدهون — `client/src/pages/Nutrition.tsx` (إضافة وجبة تعمل محليًا فقط، بدون حفظ في قاعدة بيانات)
- [x] إنشاء مكتبة التمارين (Exercise Library) المصنفة حسب المجموعات العضلية — `client/src/pages/ExerciseLibrary.tsx` (بيانات تجريبية محلية)
- [ ] ربط الشاشات الأربعة بـ tRPC + قاعدة البيانات الفعلية بدل البيانات التجريبية المحلية (`client/src/data/teekafit.ts`)
- [ ] تحسين التصميم، إضافة الرسوم البيانية، واختبار الواجهات بالكامل
- [ ] إنشاء نقطة تفتيش (Checkpoint) وتسليم المشروع

## ملاحظة مهمة

الشاشات الأربعة (Dashboard, Workouts, Nutrition, Exercise Library) مبنية بالكامل بصريًا ووظيفيًا، لكنها **غير مربوطة بأي backend** — كل البيانات محلية في `client/src/data/teekafit.ts` وتُفقد عند تحديث الصفحة (باستثناء التفاعلات داخل الجلسة نفسها كإضافة وجبة أو تحديد تمرين كمكتمل). لم تُختبر شاشة المصادقة (تحتاج `OAUTH_SERVER_URL` و`VITE_APP_ID` غير متوفرين في هذه البيئة)، لذلك تركت الشاشات مفتوحة بدون قفل تسجيل دخول مؤقتًا.
